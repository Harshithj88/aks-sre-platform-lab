replicaCount: 2

image:
  repository: acrsreplatformprod.azurecr.io/demo-api

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 6

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

env:
  ENVIRONMENT: production
