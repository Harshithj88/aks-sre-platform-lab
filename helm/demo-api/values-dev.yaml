replicaCount: 1

image:
  repository: acrsreplatformdev.azurecr.io/demo-api

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi

env:
  ENVIRONMENT: development
